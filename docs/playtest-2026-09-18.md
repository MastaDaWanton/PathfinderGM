# Play-test record: 2026-09-18 (win-unpacked b217aab, pre-0.1.10)

*The list the player handed over, item by item, with what each investigation measured and
the plan to correct them. Items 1–8 come from the first catalogued session (2026-09-08);
five of those were fixed that day and are recorded here for completeness. Items 9–23 are
from today's session on the `masta` campaign in Aurvantis. Nothing below has been built
yet: the ruling for the session was "catalogue and investigate, do not fix piecemeal".*

The save that produced most of this is `%LOCALAPPDATA%\PathfinderGM\campaigns\masta.json`;
every turn's plan, dice, repairs and rejections are in its `turn_log`, and most findings
below quote it. The raw drafts of the brothel section, against what shipped, are dumped in
the session scratchpad as `brothel.txt`.

---

## The items

### 1. `/cheat … experience` grants nothing

**Reported (09-08, again 09-18 twice).** `/cheat I gain 2000 experience` and `/cheat I
receive 2000 experience points` were echoed, an ordinary beat followed, XP stayed at 265.

**Found.** There is no op that grants experience. `xp` appears in `rules/intents.py` only
in the *claims* table (what prose may assert once the engine has awarded it, ~line 1324)
and in an `allow("xp")` rule for an op that was never defined (~1379). The engine's one
writer of XP is the end-of-encounter award (`rules/engine.py` ~6666). `cheat_messages`
hands the model the whole op reference and "Make it true", so it reached for the nearest
things: `buff type=experience amount=2000` (tell: "There is no None here to affect" — the
buff's actor was null), then `resource` on a pool called "experience" that does not
exist. The terminal line "Unknown command: /cheat" the player saw was Claude Code's own
CLI, not the game.

**Plan.** An engine-owned `xp` op with origins `author:cheat`, the encounter award and,
later, quest rewards — never the narrator's. Because the cheat's number is the author's
own, a wish naming experience, XP or levels is detected in code and the op emitted
directly, no model call. Level-up follows the existing thresholds.

### 2. A fight opened on a self-description — FIXED 09-08 (03c5294)

`judgement.redact_speech` blanks quoted spans and speech-verb complements before the nine
declaration detectors read the line. Verified live: the clerk line no longer opens an
encounter.

### 3. The damage card printed the same bonus twice and clipped its reason

**Reported.** "Damage (quarterstaff — CRITICAL)" listed "Str x2 +38" and "your modifier
+38" over a 2d6 roll; the reason line was cut mid-word inside the card.

**Found.** Not yet traced beyond the report (the character was built under Unlimited point
buy, so the size of the number is expected; the duplicate row and the clipping are not).

**Plan.** Reproduce with a high-Str critical on the dev server, read the breakdown the
roll popup receives, and remove the duplicated modifier row at its source; fix the card's
text overflow in the template.

### 4. Non-answers, then the scene drifted to the docks — FIXED 09-08

`prompts.pack` cuts the history on a stated budget and logs it; the prompt used to
overflow at turn 46 and Ollama silently ate the worked examples first.

### 5. Dialogue failed where action worked — FIXED 09-08

A `say` op with an injector and a declarer, plus `narration.unanswered_speech`, so a
spoken turn never gets the holding line.

### 6. Suggestion chips phrased as advice — FIXED 09-08

All 51 worked suggestions rewritten in first person.

### 7. A named person sat in the scene unintroduced

**Reported.** Myskalyndra Arinisyn appeared in the "In the scene" list for many turns and
was never named in prose; a quest line could be missed entirely.

**Found.** No proactivity from a scheme's cast: nothing makes a giver reach for the
player. (Related: the cards' `thread_to_pull` and `salience` machinery landed since, but a
scheme's named person is still a passive listing.)

**Plan.** A scheme's giver, once present with the player, is pushed onto the beat as the
thread to pull within two quiet turns — the same urgency machinery the cards use, with the
person's line to say, not only their presence.

### 8. "the stranger" pasted into place names — FIXED 09-08 (6291ab9)

`prompts.fill_enemy` rendered `{Current Enemy}` as "the stranger" in a worked example that
spent the token on a doorway figure; the demonstration taught the phrase.

### 9. `/gm` hands over what the character should not know, with no gate

**Reported.** The first `/gm` answer of the session: "my notes indicate that there is a
shadow power in the form of an old veterans' league that everyone in office refuses to
cross." The player's ruling: the information should be *available*, but only after the
player is asked whether they want to be told what their character does not know yet.

**Found.** The export marks hiddenness in three places: every character carries a `Secret`
fact (all 256 in Aurvantis), the world has a foundational `secret` (the narrator brief
already guards it with "never state it outright"), and cards can be `secret`. Settlement
facts carry no flag; "Shadow Power" is hidden only by its key name, "Tension" and
"Volatility" read as rumour-grade, the rest are public. Leak paths in the shipped code:
`gm_search.dossier` writes every settlement fact and puts Shadow Power among the first
four for any "who runs" question; `gm_search.passages` writes every fact of a named
person, `Secret` included; the narrator's own brief already carries Shadow Power and
Tension and the `/gm` prompt inherits it.

**Research** (every quote checked against its source). The named error is the
Alexandrian's "Preempting Investigation". GUMSHOE: "If you ask for it, you will get it";
passive clues only "when you're bogging down". Blades in the Dark: "If it's common
knowledge, the GM will simply answer your questions", otherwise a roll, and "always
answers honestly, but with a level of detail according to the level of effect". PF1e
already encodes the split the player asked for: Knowledge (local) is what you *know* —
rulers and laws DC 10, common rumour DC 15, hidden organisations DC 20, untrained capped at
DC 10, "Try Again: No" — while Diplomacy (gather information) is *finding out*: "1d4
hours canvassing", DC 10 for common facts, "20 or higher" for secret knowledge, and it can
be retried. PF2e: after a failure "further attempts are fruitless", and "some groups find
it simpler to have players roll all secret checks and just try to avoid acting on any
out-of-character knowledge, while others enjoy the mystery" — the switch is the table's.
No solo or AI tool examined (Mythic, Ironsworn, AI Dungeon, NovelAI, SillyTavern, Friends
& Fables) has a spoiler-consent or player-knowledge gate at all. Disco Elysium's passives
fire silently at a threshold and red checks cannot be retried; BG3 added "Hide Passive
Rolls" as an opt-in set once per campaign after players objected to failed checks being a
tell. Abandoned elsewhere: rolling to find clues (GUMSHOE), percentage speech checks
(Sawyer, save-scumming), always-visible failed passives (BG3). The spoiler literature is
contradictory and none of it is about play; the one robust finding is that choosing a
spoiler is the individual's and does not hurt their enjoyment — which supports offering
the choice.

**Plan.** Tier facts by key in code — public / rumour / hidden, unknown keys default hidden;
`Secret` facts and secret cards are hidden. Public answered. Rumour: a secret Knowledge
(local) roll once per (character, fact), recorded so re-asking is fruitless; on a miss,
"nothing comes to mind" plus the in-play route (ask around: Diplomacy, 1d4 hours,
retryable). Hidden: never from `/gm` by default, a hook at most ("the town's answer is too
tidy") plus the in-play route. A campaign-level setting set once — "the GM may offer what
my character doesn't know" — turns hidden into the offer the player asked for: "Your
character wouldn't know this yet — say the word if you want it anyway, or find out in
play." Known trade-off, accepted: the offer itself tells the player a secret exists.
Hidden-tier facts also leave the model's passages and the refusal's notes.

### 10. The opening was the bare template

**Reported.** "Good but very bare and short, lacking in description… if this is because
it's a cold machine then we need to warm it beforehand."

**Found.** Not a cold load. The save's own record (`transcript[0].opening`): floor true,
802 characters, problems `'Maste' is a name the material does not contain` and `it never
says who the player is; name Masta`. The model wrote the player's name with one letter
wrong; the invented-name check counted "Maste" as a name from outside the material and
the who-the-player-is check found no "Masta" — two rejections from one slip, and the draft
fell to the template. Warm-up already exists and fired: `home_views` warms the narrator
and prose models on the shelf page (14:11) and again when the forge opens; keep-alive is
45 minutes; the opening ran at 14:23. Gaps: `client.warm` swallows every exception and
logs nothing, the opening record carries no durations, only the final draft's problems are
recorded (not the first's or the repair's). Template artefact: "Vormoor keeps to A local
reeve…" — `play/opening.py` splices the export's fact mid-sentence without lowercasing it.

**Plan.** Repair a near-miss of a known name in code (edit distance one, or the same first
four letters) by substituting the real name before the checks run, instead of dropping the
draft. Raise the floor: the template quotes the place's own paragraph (already used by the
turn brief) so even a fallback describes the scene. Lowercase a spliced fact's first
letter. Record each attempt's problems and seconds on the opening; log a warm-up failure.

### 11. "…and the the stranger men are trying to contain it…"

**Reported.** Unknown men are "strangers" or "men", never "stranger men".

**Found.** The phrase is ours, twice over. The model wrote "the Reeve's men" — fair; the
town's Formal Power fact says "a local reeve". `GMAgent._world_vocabulary` collects only
*capitalised* words from the world's text; the export says "reeve" in lowercase and the
model title-cased it, so a word the world uses constantly read as a person from nowhere
(same class as the Council/Elders false positives: of 31 flags measured earlier, 4 were
real). Then `narration.unname_strangers` replaced the flagged token with "the stranger";
the token it received already carried the possessive, so the 's was dropped and the
article already in front was never looked at: "the Reeve's men" → "the the stranger men".

**Plan.** A capitalised token is invented only if its lowercase form appears nowhere in
the world's text, the scene or the rules (measure on the two 60-turn audits first: the
genuine inventions — Kaida, Vorgath, Keldor — appear in no world text and stay caught). In
the renamer, a possessive after an article drops the owner and keeps the noun ("the men
are trying to contain it"); without an article it becomes "the stranger's men"; never
"the the", never "stranger men". A test on the exact sentence, both ways.

### 12. An NPC asked for his name named himself with our placeholder

**Reported.** "Let's call it the stranger." Everyone should have a name; refusing to give
it is fine, but "the stranger" is obviously the result of not having one; and a name given
in play must update the scene list.

**Found.** The turn's repair log: `invented-name: names 'Kaelen'` → `people from nowhere:
un-named Kaelen`. The model gave him a name; the renamer struck it inside his own line.
Nothing on an actor holds a real name behind the descriptor ("the stranger sharing the
step", guildhand, human); the scene panel shows `actor.name`; no op renames an actor;
`established_names` keeps a name only after two GM beats and the renamer strikes it on the
first, so a name given in play can never establish. **The material exists and is unused:**
the export ships `play.names` — 80 name pools, each `{people_id, given[16+],
family[16+]}`; 16 resolve to PEOPLE entities (Human, Elf, Gnome under Kelmoria; Orc,
Half-Orc, Goblin, Hobgoblin under Drossakar), 64 to finer culture ids. No module in
`play/`, `rules/` or `gm/` reads them. "Kaelen" is a hair from the world's own
Kael/Kaelin/Kaelos — the model was guessing at the pool it was never shown.

**Plan.** Every actor gets a true name at spawn or promotion, drawn from its people's pool
(fallback: the settlement's or nation's dominant people, then any pool), kept beside the
display name; the panel shows the descriptor until the name is given in play. The brief
carries it as "gives the name Kael Vorlix if asked" (and a world resident's real name).
An introduction is detected in code — "call me X", "my name is X", "the name's X", "they
call me X" — from an unnamed actor present: the display name becomes X and X joins the
known names; if the model used some other name, the assigned one is substituted, never
"the stranger". The renamer never places its descriptor inside a character's own
introduction. Refusing to give a name stays legitimate.

### 13. A sunder with no damage roll; "the the stranger primary torso"; "the beast"

**Reported.** "I never rolled damage to break his weapon", and whichever of us "the beast"
meant, it is wrong.

**Found.** (a) The sunder *did* roll, once: the manoeuvre check, 12 + 20 = 32, success by
21, tell "you damage an item the target is holding or wearing". That tell is the whole
effect: `rules/tables.py` marks sunder `damages_item: True` and nothing in
`rules/engine.py` reads that key — manoeuvre success applies only conditions (~2844-2856).
The item-damage machinery exists (`_object_damage` ~6194, `Actor.damage_item`, items with
hardness and hp per `rules/sheet.py:123`, broken/destroyed states) but is wired to effect
specs (acid on a scabbard), not to the manoeuvre. So the club took nothing, no weapon
damage roll was offered, and the prose — told "success" and nothing about how much —
decided the club was in fragments. (b) "the the stranger primary torso" is item 11's
renamer inside the NPC beat: the PC's `heritage` is blank (the homebrew race sits in
`race`) and `_known_names` adds `heritage`, never `race`, so "Asura" is an invented person.
Consistent, not proven: the npc-turn log records intents and outcomes but not repairs. (c)
"the beast" is the model's own word for the PC — in the NPC plan's motive and the miss
beat — from no content or prompt. The brief gives the model nothing to call the PC by
kind: heritage blank, and the homebrew card's body line renders *empty* (`races.body_line`
returns "" because the card's effects were never converted), so its bite, claws, tail,
gills and extra arms never reach the narrator as facts. Left with a nameless-race brawler
at 70 hp and a +19 Str modifier who punched a club, it reached for "the beast"; "primary
torso" is the same guesswork.

**Plan.** Wire sunder to the object model per PF1e: on a hit, roll the weapon's damage
against the item (player-visible when the PC swings), apply hardness, broken at half hp,
destroyed at zero; source the exact text from legacy.aonprd.com combat/sunder when
building; the tell names the state (dented / broken / destroyed / in pieces) and a
contradiction check holds the prose to it. Add `race` and the race document's name to the
known names; log NPC-turn repairs. An NPC beat or motive refers to the player's character
by name or "you", never by a creature noun unless the race document says so — a mechanical
substitution. The brief carries a body line for a homebrew race even when unconverted (its
type, size and the evolutions it lists by name), and the PC line never renders with a
blank heritage.

### 14. The fight began with the challenger and was resolved against the stranger, who died

**Reported.** "I start the fight with one guy and it somehow midway becomes the stranger
sharing the step, and now a character who had nothing to do with the fight is dead."

**Found.** The challenger *did* become an actor — c6, "desperate man", a guildhand promoted
by the cast ledger from a prose phrase (the stray adjective, not the player's description
of him). The first attack plan (`turn_log[18]`) chose c1: `attack c1, because "you strike
his weapon to disarm or stun him"`. The brief listed both as guildhands with clubs — c1 the
person the player had spoken to all scene, c6 a "desperate man" whose name matched nothing
in the prose about the challenger — and nothing in the plan path checks the target against
who the player is engaged with. `fill_obvious_targets` fills only a *missing* target and
only when exactly one fightable candidate stands (its own principle: "two is a choice, and
choosing for the player is worse than asking them"); here the model supplied a target and
seven promoted civilians all counted as fightable. `update_thread` had "wait for a
challenger" as a subject bound to nobody, "tell him to come at me" continued it on a bare
pronoun, and `begin_encounter` then cleared the thread. From the wrong first choice
everything followed correctly: c1 swung back, "throw it at the man" went to the
established opponent, a 4-hp civilian died to a thrown chunk of wood, and the kill paid XP
for him.

**Plan.** Bind arrivals to the engagement in code: when the thread's subject is a role
with no actor and a person is promoted from the next beats, that ref becomes the subject,
survives `begin_encounter` as "opponent", and the brief names it. Check the plan's target
before validation: a player line whose target is a pronoun or definite common noun with a
plan aimed at an actor the recent beats never mention, while the engaged actor is
mentioned, is repaired to the engaged actor with the fix named; when two candidates are
genuinely live, hand back the question ("which man — the stranger who shared your step, or
the challenger with the spiked club?"). A promoted actor is named by the prose's
description; one the prose says challenges or squares off is marked an opponent. Log the
candidates and the reason for the choice on the turn.

### 15. "I sundered the other guy's weapon and now it thinks it was a table"

**Reported.** "…the smoldering wood of the table still glowing faintly where his final
strike landed." Then: "the owner of the item needs to be tracked as well."

**Found.** A drift chain, every link prose-owned. [20] "the club shatters" — prose only;
the engine's club never changed (13a). [23] player: "i pick up a chunk of wood". [24] "The
wood is still smoldering from the impact" — fire invented from nothing, and the same turn's
plan tried `cast` ("Masta is a blood bending and does not cast spells") and `use_ability`.
[27] the next beat read its own [24] as fact and rationalised it into "the table", adding
"singed hair". Nothing anchors objects or physical states: the cast ledger grounds people,
the invented-name check grounds names, the contradiction check grounds conditions and
deaths — no check grounds *things*. Ownership exists only by containment: an `Item`
carries hardness and hit points inside an actor's gear, nothing about whose it is or what
it was; `loot`, `give` and `found` move things between people; nothing exists off a person
— a dropped weapon, fragments, a chunk of wood on the ground have no record.

**Plan.** 13a first (it removes the seed). Ground physical states the engine does not
hold: fire vocabulary with no fire source in the scene is an invention — detected in code,
rewritten once with the fix named, cut as the backstop. A props ledger per spot for
objects the prose introduces, fed back as fact. **Every object the fiction touches carries
an owner, a provenance and a state** — `owner` (the holder; for ground props, the last
holder or the merchant/place it belongs to), `from` ("fragments of the challenger's
club"), `state` (intact / broken / destroyed / fragments) — kept through every transition
by one applicator: sunder leaves fragments at the spot owned by the wielder; drop, loot,
pick up, give and steal move the same record; picking up keeps the provenance. Taking what
belongs to someone present feeds the existing property heat. The brief lists what lies at
the spot and whose it is. A thrown improvised object is an improvised-weapon attack in the
plan grammar, never a cast.

### 16. Attacked first, and no fight began until the player swung

**Reported.** "I should be put into combat when I am attacked, it shouldn't wait for me."

**Found.** The only door into an encounter is the `begin_encounter` op, which the *model*
must emit; `judgement.inject_fight` opens one in code only when the *player* starts it, and
`judgement.joiners` reads "comes at you" from a beat only when an encounter is already
running. So when the man in the leather apron swung "at your guard with a heavy,
horizontal sweep" (beat 33), the plan was `narrate_only` + `say`, the beat had him swing,
and the engine rolled nothing. He also never became an actor: `note_cast` deduplicates a
new person on the head word of their phrase against every actor name on the board — "a
man with a scarred face…" ends in "man", "desperate man" already stood, so he was skipped.
On the sunder turn (`turn_log[36]`): "a sunder needs a target" five times, then the model
took `_SPAWN_HINT` literally and created a **thug named "weapon"** (c10) and attacked it —
"Battle is joined: Masta squares off against weapon." That actor is on the save's
initiative list.

**Plan.** An NPC-side fight opener in code, the mirror of `inject_fight`: when a GM beat
has a non-player actor (or an unbooked person it introduces) performing an attack verb at
the player, the engine opens the encounter with sides, rolls initiative with him acting
first (PF1e surprise round / initiative, to be sourced), and resolves *his* attack with
dice before the player's next line; a described blow with no attack outcome behind it is a
contradiction finding, rewritten or cut. `note_cast` dedups on head word only when the
whole phrase repeats; a second man with a different description is a second person. The
spawn hint and the sunder legality name the *person* missing from the board and never
invite spawning a thing; a spawn named after an object is refused; a way to remove a
mis-spawned actor from a save.

### 17. "Either a dead man is swinging or some other error is happening"

**Reported.** The desperate man dies twice in one beat; then "The weapon lashes out…";
then "Your blade whistles through the air, but the heavy iron of the guard's shield…".

**Found.** Not a dead man. The swinger is c10, the thug named "weapon", alive at 13 hp,
taking its NPC turn ("Attack with sap 8 against AC 22", miss); the beat calls it by its
actor name. The pebble went to the wrong man again (14): the apron man still had no actor,
so "him" resolved to c6, killed at -17 by an unarmed strike (37 to hit, 21 damage). New
(a): the consequence line inverted who struck whom — the engine's tell was "weapon's attack
misses Masta"; the model, reading an attacker called "weapon", wrote the player attacking a
guard; no check holds the attacker/defender roles of a tell through the prose. New (b):
two deaths in one beat — the model wrote the collapse without the word "dead",
`death-left-off-the-page` fired and the backstop *appended* a death line from the
bludgeoning/huge-margin pool ("the head is simply gone… the body stands, spraying") after a
beat that had already laid him in the dirt with a caved cheek; a pebble did that.

**Plan.** Agency is a fact of the tell: the attacker stays subject and the defender
object — detected mechanically ("your blade/strike" forbidden when the attacker is not the
player), rewritten once, the plain tell as backstop. `death-left-off-the-page` accepts a
described death (collapses and does not move, lies still) or *replaces* the beat's own
ending rather than appending a second death; the death pool takes the weapon into account
— a pebble or a fist does not take a head off (bucket by damage family, margin and weapon
size). 16c, 16b/14b first; the fallen leave `initiative` and `sides` when they drop.

### 18. The board is scenery

**Reported (on seeing the grid).** "The weapon is an attacker with 13hp?" — yes.

**Found.** Nine non-player actors on a five-by-five map: Drenn, seven promoted bystanders
(guard, local guards, traders, nearby merchant, nearby merchants, boy, desperate man) and
the thug named "weapon". `promote_cast` spawns every noted person as a guildhand or
watchman with hit points and a club; every one is a fightable target for the plan and a
candidate for "him". The cap of four counts only entries still in the ledger, and the
ledger is cleared after each fight, so the count restarts while the actors stay.

**Plan.** Bystanders promoted from prose stay civilians — present, addressable, tradeable,
but not attack candidates and not on the initiative until they act or are acted on; the cap
counts standing promoted actors, not ledger entries; the plan's target list offers only
opponents and whoever the player named.

### 19. "Killing the weapon here"

**Reported.** "The fight is over. You gain 200 XP for weapon, desperate man."

**Found.** `turn_log[44]`: "Masta hits weapon for 20 bludgeoning. weapon is unconscious and
dying" — c10 at -7/13, not dead; the prose matched the engine for once. The encounter
ended with no conscious hostile left and the award paid for both "foes": the thug named
after an object (16c) and the bystander who was never in the fight (14). Nothing new
mechanically; two notes: the award line prints raw actor names, and the fight ends while a
foe is dying yet the prose offers a finishing blow.

**Plan.** 16c and 14; the dying and the dead leave `initiative` and `sides` cleanly; the
prose does not offer a coup de grâce as if the fight were still on, or the encounter stays
open one beat for it.

### 20. `/cheat I gain 2000 experience` still does nothing

Item 1, reproduced twice; the measured cause is recorded there.

### 21. "The woman is not described at all"

**Reported.** A paragraph on the room; of the woman only her gaze and manner.

**Found.** No guard asks for or checks a description when a person first appears; the body
checks stop the *wrong* body, nothing requires *a* body. The brief carries no appearance
for anyone: the world's residents have one — Drenn Ironvale's Appearance fact is "favors
plain dress that makes their occasional fine piece of jewelry impossible to miss" — and it
reaches `/gm` but never the turn brief. PEOPLE entities carry Anatomy / Body / Senses;
settlements carry dress cues in some worlds. A promoted civilian has no appearance at all,
so every later beat is free to re-invent her.

**Plan.** A first-appearance check: a newly booked person whose introducing sentences carry
no appearance vocabulary is a finding — one rewrite with the fix named ("two or three
things a stranger would see"), and as the deterministic backstop a grounded line from the
people's Anatomy and the settlement's dress cues. Appearance is a fact in the brief: a
resident's own, a civilian's attached at promotion with the name (12), and kept. The first
meeting with a named resident must use their Appearance fact.

### 22. The paid encounter that never happened

**Reported.** Negotiated plainly, paid, undressed, waited, told her to get on with it — and
got seven beats of steam, jasmine and a drink. "Check through the interactions and find out
what the hang-up is."

**Found** (raw drafts against shipped beats, the whole section). **The model was not the
hang-up.** Its first drafts were willing and progressing at [79] ("The girl doesn't flinch
as you pull her onto you… settles against you, her weight a warm presence against your
chest") and [81] ("her hands steady on your tunic as she works to discard the layers
between you"), and its plans said so ("the scene shifts to the physical act of the
transaction"). Five of our mechanisms stopped it:

- **Continue.** Beats 62, 64, 66 and 71 were Continue presses. Continue's worked examples
  demonstrate, by design, "nobody acts, nothing new arrives, the place and the people
  already here go on" — so the model wrote exactly that. Its own text leaks too: the
  planner read the CARRY_ON instruction as the player's line — `say` of "in their own
  words, and let the people and the place here go on…", `give item "scene on"` three times
  ("Masta takes scene on"), `use_item potion_of_rest_01`.
- **The polish rewrite.** Every prose turn was under the 800-character floor and went to
  `polish`, whose drafts replaced her *actions* with atmosphere: [70] raw "The woman
  continues her work…" → polish "The timberer's rhythmic thud…"; [79] the progressing raw →
  "The rhythmic thud of the timberer's mallet… Steam rises… The woman with the basin remains
  a shadow". The repair pass is where the scene was neutered.
- **The escape-claim guard.** [81]: "She doesn't pull away; instead… she works to discard
  the layers between you" matched `(?:slip|break|pull|…)\s+(?:free|loose|away|out)`
  (`rules/intents.py` ~1186) — "pull … away", *negated* — as "states an escape"; the
  sentence was cut, the beat fell to 504 characters, polish again.
- **The phrase guards.** `formulaic-opening` on "the woman"/"the girl" and
  `recurring-phrase` on "is thick with the" fired on nearly every beat of a two-person
  room, each a content-losing rewrite.
- **The context budget.** "dropped the oldest 17–29 messages" every turn from [69]: the
  negotiation and the payment had left the window; nothing carried the agreement forward.

Also: the ten gold never left the purse (`sell gold_coins_10` → "Masta is not carrying
gold_coins_10") while the prose took the coin; the watcher advanced the water-rights strain
card from inside the brothel ("the current transaction is a private matter") and paid 200
XP for it. There is no content setting anywhere; the app has taken no position, so the
outcome is whatever the guards happen to do.

**Plan.** A content house rule set once per campaign — explicit, or fade-to-black as a
designed transition (time passes, the scene resumes) rather than a stall; either way the
guards must not decide it by accident. Continue per item 23. Polish keeps the events: a
length or repetition rewrite must preserve every action sentence of the draft, or the
draft ships at its own length (the floor is for prose that is *wrong*, not less — the
opening rule of 09-18). The escape claim respects negation and requires a grapple or
restraint on the books. The phrase guards exempt the only other person present once the
cast is two. The agreement survives the window: a scene-level "what was agreed" fact
written by the engine when a payment lands, carried in the brief until the scene changes
(docs/memory-policy.md's gap in its most concrete form). Pay by amount from the purse; the
watcher may not advance a settlement strain card from a private scene.

### 23. RULING — what Continue means

**The player's words.** "Continue should work as I have nothing to add and continue the
scene, not as nobody does anything. My character should keep doing whatever he is doing
and the scene should move forward without any addition from me. If I am running a machine
and a fight breaks out, if I then hit continue my character should keep running the
machine as more of the fight plays out, and if I keep hitting continue the fight should
play out further and further, even coming to an end, having the authorities called. The
whole time I'm hitting continue I will be running the machine."

**Found.** The shipped design is the inverse (`gm/prompts.py` CARRY_ON and
CARRY_ON_EXAMPLES: "nobody acts, nothing new arrives").

**Plan.** The player's character *continues their standing action* — the thread's "doing"
— stated to the narrator as a fact of the beat, never dropped, never turned into a new
action. *The world advances one beat*: the engaged NPC goes on with what they were doing;
in a fight the round proceeds with the NPC turns rolled and the player's turn a legal delay
(or total defence, by setting); the watcher ticks; a card may escalate; time passes. Each
press advances further — a fight left to run reaches its end and its consequences on its
own. Continue is a *directive*, never an utterance: its text never reaches the planner as
the player's words, and `say`/`give` of the directive's own text are refused. The Continue
examples *show* a standing action held while the scene moves; none may show a stalled
room.

---

## The plan, in order

Grouped by root cause, because most of the fifteen new items are five defects seen from
different sides. Each group is one branch off master, one full suite, a live check through
the real path (`/api/say` on a copy of the `masta` save — the live loop is what found the
real defect in `/gm` after the unit tests were green), then merge. Nothing ships to 0.1.10
until the player has replayed the same scenes on a fresh `win-unpacked`.

**1. The fight is the engine's, from either side** — items 16, 14, 18, 17, 19, 13a.
BUILT 2026-09-18 on branch `fight-from-either-side`; design, tests and the three live
replays measured in `docs/fight-from-either-side.md`. Awaiting the player's own replay
before merge.
Order: 16(a) the NPC-side opener, because everything downstream of a fight that never
opened is wrong; 16(b)/(c) the head-word dedup and the spawn hint; 14 the engagement
binding and the target check with the hand-back question; 18 civilians are not
combatants; 13(a) sunder wired to the object model with the state in the tell; 17 agency
held through the prose and the death backstop; 19 the dying leave initiative. Live check:
the ring fight replayed — the challenger is booked as a person, he swings first and the
fight opens on his swing, the sunder rolls damage against his club and the tell says what
became of it, the bystanders never enter the roster of targets.

**2. Things and their owners** — items 15, 13a (shared), 22's payment.
BUILT 2026-09-18 on branch `things-and-owners`; design, prior art, tests and the live
replay in `docs/things-and-owners.md`.
The props ledger per spot with owner / provenance / state through one applicator; fire
grounding as an invention check; the improvised-weapon attack in the grammar; pay by
amount from the purse. Live check: the chunk-of-wood sequence — the fragments lie at the
spot owned by the challenger, picking one up keeps its provenance, nothing smoulders, and
the next beat cannot make it a table.

**3. People have names and faces** — items 12, 21, 13c.
BUILT 2026-09-18 on branch `names-and-faces` (item 11's renamer fix done here too);
design, tests and two live replays in `docs/names-and-faces.md`.
`play.names` read for the first time: a true name at spawn or promotion from the people's
pool, an appearance attached beside it, both in the brief as facts; the introduction
detector renames the panel; the first-appearance description check; the body line for an
unconverted homebrew race; `race` in the known names; NPC beats never call the player a
creature. Live check: the stranger on the step asked his name gives one and the panel
updates; a woman in a doorway is described in two or three particulars that persist.

**4. Continue, polish and the guards that over-reached** — items 23, 22, 11, 10.
Continue redesigned as the ruling states, with new demonstrations; the directive kept out
of the planner; polish required to keep every action sentence; the escape claim with
negation and a restraint requirement; the phrase guards exempting the sole other person;
the world vocabulary case-blind and the renamer's possessive fix; the near-miss name repair
and the raised template floor. Measured before and after on the two 60-turn audits and on
the brothel section replayed: the count of action sentences lost to polish, guards firing
per beat, and — the number that matters — beats where the scene moved.

**5. The GM's knowledge and the table's content** — items 9, 22's setting, 22's memory.
The fact tiers and the once-per-campaign settings (knowledge offer; content), the secret
Knowledge (local) roll with the no-retry ledger, the in-play route named on a miss, the
"what was agreed" fact carried in the brief. Live check: "who runs this town" answers the
reeve and withholds the veterans' league until asked for; a paid arrangement survives
twenty turns of context pressure.

**6. The engine's small doors** — items 1/20, 3, 7, 22's watcher scope.
The `xp` op and the cheat's mechanical detection; the damage card's duplicate row and
clipped reason; scheme proactivity for a giver present; the watcher barred from advancing a
town's strain card from a private scene.

Then the release order in docs/packaging.md: bump, PyInstaller clean, `npm run dist`,
`prove_build.py`, `prove_shell.py` and `--packaged`, tag after building, draft the release
with `latest.yml`, upload the installer detached, publish.
